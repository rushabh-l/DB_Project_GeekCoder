# The main pipeline for DB_Project_Bundle

resources:
  pipelines:
    bronze_silver_earthquake_etl:
      name: Bronze_Silver
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob :
           include: ../src/DLT_pipelines/Bronze_Silver/transformations/**
      schema: silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/DLT_pipelines/Bronze_Silver




